# Calculator
🔢 Logic CalculatorA fully functional calculator for daily arithmetic.Features: Basic operations ($+$, $-$, $\times$, $\div$), decimal support, and percentage calculations.Tech Highlight: Implements a custom expression parser to handle order of operations.
🔢 Calculator Input: Use your mouse to click the on-screen buttons or use your keyboard number pad (if supported).

Clear (AC): Use the “AC” or “C” button to wipe the current calculation.

Result: Press the = key to execute the logic. The calculator follows standard BODMAS/PEMDAS rules.

